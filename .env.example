# Redis (Phases 0–5) — optional; API works without it (in-memory fallback)
REDIS_ENABLED=false
REDIS_URL=redis://127.0.0.1:6379/0
# REDIS_KEY_PREFIX=nq

# Local Redis: docker compose -f docker-compose.redis.yml up -d
